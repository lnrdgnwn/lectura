import RegisterForm from "../Components/Register/RegisterForm";

export default function Register() {
  return (
    <section id="register" className="flex w-full justify-center min-h-screen">
      <RegisterForm />
    </section>
  );
}
